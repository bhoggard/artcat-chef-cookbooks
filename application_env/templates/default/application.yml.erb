# This file is generated by the application_env cookbook. Update it by setting the
# stack's custom JSON and executing the application_env::configure recipe.

<% @env.sort.each do |key, val| %>
<%= key.to_s %>: <%= (val && val.to_s.inspect) %>
<% end %>
